import { Box, Card, Stack, Input, Textarea, Button } from '@kalidao/reality'
import { preview } from '@design/blur.css'

const Preview = () => {
  return <Box className={preview} padding="6"></Box>
}

export default Preview
